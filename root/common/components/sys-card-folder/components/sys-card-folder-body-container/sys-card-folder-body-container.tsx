import React, { FunctionComponent, memo } from 'react';

const SysCardFolderBodyContainer: FunctionComponent = () => {
  return <div className="sys-card-folder-body-container" />;
};

export default memo(SysCardFolderBodyContainer);
