import React, { FunctionComponent, memo } from 'react';

const SysCardFolderActionsBar: FunctionComponent = () => {
  return <div className="sys-card-folder-actions-bar" />;
};

export default memo(SysCardFolderActionsBar);
