import React, { memo } from 'react';

import { Box } from 'components/index';

const SysCardFolderActionsBar = () => (
  <Box className="sys-card-folder-actions-bar" />
);

export default memo(SysCardFolderActionsBar);
